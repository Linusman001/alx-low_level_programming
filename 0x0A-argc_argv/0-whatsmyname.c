#include <stdio.h>
#include <stdlib.h>

/**
 * main - program that prints its name
 * followed by a new line
 * Return: 0
 */
int main(int argc, char* argv[])
{
	printf("%s", argv[0]);
	return (0);
}
