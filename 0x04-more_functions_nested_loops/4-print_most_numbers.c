#include "main.h"

/**
 * print_most_numbers - prints numbers from 0 - 9
 * followed by a new line
 * Return: 0
 */
void print_most-numbers(void)
{
	int a;

	for (a = '0'; a <= '0'; a++)
	{
		if (a != '2'&& a != '4')
		{
			_putchar(a);
		}
		_putchar('\n');
	}
}
