#include <stdio.h>

/**
 * main - Print letters in lowercase
 *
 * Return: Always 0
 */
int main(void)
{ char alphabet;

	while (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
		putchar(alphabets);

		putchar("\n");
	}

	return (0);
}
